<%= session[:service_id] %>
<div class="container">
  <div class="row align-items-start">
    <div class="col-12">
      <h1 class="h3 mb-3 font-weight-normal text-center">Retrouvez tous nos artistes</h1>
    </div>
  </div>
</div>
<div class="container">
  <% @artists.each do |artist| %>
    <%= render 'card', artist: artist %>
  <% end %>
</div>
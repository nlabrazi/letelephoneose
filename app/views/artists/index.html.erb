
<h1>Listing artists</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @artists.each do |artist| %>
    <tr>
    	<td>
    	
    	<img src="<%= artist.user.avatar %>" alt="artist picture" style="max-width:100%; max-height: 20vh;" class="mx-auto">
    	
    	</td>
      <td><%= artist.name %></td>
      <td><%= artist.description %></td>
      <td><%= artist.user.email %></td>
      <td><%= link_to 'Show', artist %></td>
      <% if policy(artist).edit? %>
        <td><%= link_to 'Edit', edit_artist_path(artist) %></td>
      <% end %>
    </tr>
    <% end %>
  </tbody>
</table>

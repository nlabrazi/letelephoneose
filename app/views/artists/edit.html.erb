<h1>Editing Artist</h1>

<%= simple_form_for @artist do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>

<%= link_to 'Show', @artist %> |
<%= link_to 'Back', artists_path %>

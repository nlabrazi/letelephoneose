<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @artist.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @artist.description %>
</p>

<p>
  <strong>User:</strong>
  <%= @artist.user.email %>
</p>

<% if policy(@artist).update? %>
  <%= link_to 'Edit', edit_artist_path(@artist) %> |
  <%= link_to 'Revenir à la liste des artistes', artists_path %>

  <%= link_to 'Réserver cet artiste', artist_availabilities_path(@artist.id) %>
<% end %>
